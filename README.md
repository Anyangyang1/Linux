# Linux网络篇
```
  arp -a                               # 查看arp高速缓存
  sudo arp -d IP地址                    # 删除arp缓存
  sudo arp -s IP地址   MAC地址          # 增加arp缓存

  sudo tcpdump                          # 抓取数据包

  route                                 # 查看路由表
```

